spring:
  application:
    name: talentbridge-api

  datasource:
    url: jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration
      - org.springframework.boot.autoconfigure.data.redis.RedisRepositoriesAutoConfiguration

  cache:
    type: simple

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  ai:
    anthropic:
      api-key: ${ANTHROPIC_API_KEY}
      chat:
        options:
          model: claude-sonnet-4-6
          max-tokens: 4096

server:
  port: 8080
  servlet:
    context-path: /api

app:
  jwt:
    secret: ${JWT_SECRET}
    expiration-ms: ${JWT_EXPIRATION}           # e.g. 86400000 for 24 hours
    refresh-expiration-ms: ${JWT_REFRESH_EXPIRATION}  # e.g. 604800000 for 7 days

  onet:
    base-url: https://api-v2.onetcenter.org
    api-key: ${ONET_API_KEY}
    cache-ttl-hours: 24

  cors:
    allowed-origins: ${CORS_ORIGINS}

  frontend:
    url: ${FRONTEND_URL}

  reset-password:
    expiration-minutes: 30

  aws:
    region: us-east-1
    s3:
      bucket: ${AWS_S3_RESUME_BUCKET}
    ses:
      from-email: ${AWS_SES_FROM_EMAIL}
  branding:
    talentbridge-logo-url: ${TALENTBRIDGE_LOGO_URL}
    jgc-logo-url: ${JGC_SOLUTIONS_LOGO_URL}

logging:
  level:
    com.talentbridge: INFO
